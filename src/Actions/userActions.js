import { GET_USER, ADD_USER, DELETE_USER, VALIDATE_USER } from './types';

import axios from 'axios';

export const validateUser = email => {
  // axios.post
  //.catch
        //payload: //result from the post request'
  
}